# BaxeAI 프로젝트 요구사항
# 기본 패키지 (모든 환경 공통)
numpy>=1.21.0,<2.0.0
opencv-python-headless>=4.5.0,<4.12.0
pandas>=1.3.0,<2.0.0
matplotlib>=3.4.0,<3.8.0

# OS별 SciPy 패키지 (Python 버전 호환성 고려)
scipy>=1.11.1,<1.12.0; platform_system == "Linux" and python_version >= "3.12"  # Linux Python 3.12+
scipy>=1.11.1,<1.12.0; platform_system == "Windows" and python_version >= "3.12"  # Windows Python 3.12+
scipy>=1.11.1,<1.12.0; platform_system == "Darwin" and platform_machine == "arm64" and python_version >= "3.12"  # Mac ARM (M1/M2/M3) Python 3.12+
scipy>=1.10.0,<1.11.0; platform_system == "Darwin" and platform_machine == "x86_64" and python_version >= "3.12"  # Mac Intel Python 3.12+
scipy>=1.7.0,<1.10.0; python_version < "3.12"  # 모든 OS의 Python 3.11 이하 버전용

# 머신러닝 패키지
scikit-learn>=1.0.0,<1.4.0
numba>=0.53.0,<0.61.0

# OS별 MediaPipe 패키지
mediapipe>=0.10.13; platform_system == "Linux" and python_version >= "3.12"  # Linux Python 3.12+
mediapipe>=0.10.13; platform_system == "Windows" and python_version >= "3.12"  # Windows Python 3.12+
mediapipe>=0.10.13; platform_system == "Darwin" and platform_machine == "x86_64" and python_version >= "3.12"  # Mac Intel Python 3.12+

# Mac M1/M2/M3 (ARM64) 전용 MediaPipe 대체 패키지
opencv-contrib-python>=4.7.0,<4.12.0; platform_system == "Darwin" and platform_machine == "arm64"  # Mac ARM용 OpenCV 확장

# 하위 버전 호환용 MediaPipe
mediapipe>=0.9.0,<0.10.0; python_version < "3.12" and platform_system != "Darwin"  # Linux/Windows Python 3.11 이하

# Vercel 환경용 추가 패키지
Cython>=0.29.0; platform_system != "Darwin" or platform_machine != "arm64"  # Mac ARM을 제외한 모든 환경
