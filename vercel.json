{
  "buildCommand": "NODE_OPTIONS='--max-old-space-size=4096' pnpm install && node vercel-build.js && pnpm run vercel-build",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "outputDirectory": ".next",
  "regions": ["icn1"],
  "env": {
    "PYTHON_VERSION": "3.12"
  }
}
